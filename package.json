{
  "name": "mcoe-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCOE docs — content + config rendered by the Nebula Docs CLI.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "nebula dev",
    "build": "nebula build",
    "preview": "nebula preview",
    "validate": "nebula validate"
  },
  "devDependencies": {
    "@nebula-docs/cli": "0.0.1",
    "firebase-tools": "^13.0.0"
  }
}
